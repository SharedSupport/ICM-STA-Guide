{
  "name": "icm-program-portal-api",
  "private": true,
  "version": "0.1.0",
  "main": "dist/api/src/index.js",
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -p . --watch",
    "start": "npm run build && func start",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@azure/cosmos": "^4.2.0",
    "@azure/functions": "^4.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
